[package]
name = "vernier-patterns"
version = "0.1.0"
edition = "2024"

[dependencies]
vernier-core = { path = "../vernier-core" }
vernier-render = { path = "../vernier-render", optional = true }

[features]
vulkan = ["dep:vernier-render"]
